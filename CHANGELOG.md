## 0.0.2

* Upgrade to noir-rs `v1.0.0-beta.19` (migrates to the official `barretenberg-rs` crate).
* Regenerate the `noir_multiplier2` example fixtures (`.json`/`.srs`/`.vk`) for beta.19 — the oracle hash is now baked into the verification key.
* Android: the barretenberg prebuilt is linked via Zig and requires `minSdkVersion >= 30`.

## 0.0.1

* TODO: Describe initial release.
